<!-- nav_summary: Install & Configure Claude Code (OS/CLI + Server Console) -->
The **System Configuration & Setup** section provides end-to-end guidance for deploying and managing Claude Code, covering both local and server-managed environments. It begins with detailed instructions for installing Claude Code across operating systems (OS), configuring the command-line interface (CLI), managing updates, and authenticating users. For organizations leveraging server-managed deployments (public beta), it outlines centralized configuration via a web-based console, enabling administrators to enforce settings, monitor usage, and streamline deployments at scale.