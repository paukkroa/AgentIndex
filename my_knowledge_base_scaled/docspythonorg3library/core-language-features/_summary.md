<!-- nav_summary: Core types, memory, enums, and FP tools overview -->
The **Core Language Features** section provides an in-depth exploration of Python’s fundamental building blocks, covering essential data types, memory management, type utilities, and functional programming constructs. It begins with an overview of **core built-in types and constants**, including their roles and exceptions, followed by **memory-efficient references** and **dynamic type utilities** to optimize performance and flexibility. The **`enum` module** section details Python’s support for enumerations, a key feature for defining symbolic values. Additionally, it examines **functional programming tools** like `itertools`, `functools`, and `operator`, which enhance modularity and expressiveness. The section concludes with an **introduction and navigation guide** to core Python types and standard library modules, ensuring developers can efficiently locate and utilize foundational components.

---