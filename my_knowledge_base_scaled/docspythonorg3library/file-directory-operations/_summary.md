<!-- nav_summary: Python filesystem tools: paths, copies, temp files, comparisons -->
The **File & Directory Operations** section provides a comprehensive overview of Python’s built-in modules and techniques for managing filesystem interactions, including path manipulation, file/directory comparisons, temporary file handling, pattern matching, metadata interpretation, high-level operations (copy/move/delete), and efficient text line retrieval. It covers core modules like `os.path`, `filecmp`, `tempfile`, `glob/fnmatch`, `stat`, `shutil`, and `linecache`, offering both low-level control and high-level abstractions for robust filesystem automation.